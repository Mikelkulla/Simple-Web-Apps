//  objects - store data in-depth -composite / complex data type
//  key-value pairs

let castleListing = {
  isSuperHost: true,
  name: "My Castle",
  price: 190,
  amenities: ["Bathroom", "Kitchen", "Balcony"],
};

console.log(
  castleListing.name,
  castleListing.isSuperHost,
  castleListing.price,
  castleListing.amenities
);
